package com.skillstorm;

public enum Day {

	// provided methods
	// - name() -- return String
	// - ordinal() --  returns an int
	
	// valueOf (class, String)
	// static valueOf (String)
	// values() -- returns all the values so I can iterate over them
	
	MONDAY, // 0 is returned when .ordinal() is called    and "MONDAY" when name() is called
	TUESDAY, // 1
	WEDNESDAY, //2
	THURSDAY, // 3
	FRIDAY, // 4
	SATURDAY, // 5
	SUNDAY // 6
	
}
